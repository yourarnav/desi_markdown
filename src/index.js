import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Optional, if you plan to use styles
import MarkdownNotesApp from './App';

ReactDOM.render(
  <React.StrictMode>
    <MarkdownNotesApp />
  </React.StrictMode>,
  document.getElementById('root')
);
